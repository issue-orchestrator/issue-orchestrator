**Audience:** Design document (public). Not a usage guide.

# Architecture Guardrails

We enforce import boundaries with `import-linter` to keep core logic dependency-light and testable.

Run:
```bash
pip install import-linter
import-linter
```
