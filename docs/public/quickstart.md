# Quickstart

(Placeholder / template)

1) `issue-orchestrator setup`
2) Configure `.issue-orchestrator.yaml`
3) `issue-orchestrator run` (or your primary command)
4) Use GitHub labels/issues as the source of truth; orchestrator manages worktrees/sessions.
