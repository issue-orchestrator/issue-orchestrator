# Installation

(Placeholder / template)

- Create a virtualenv (recommended)
- `pip install -e .`
- Run: `issue-orchestrator --help`

If the repo uses extra dependencies (tmux/iTerm/web UI), document them here.
