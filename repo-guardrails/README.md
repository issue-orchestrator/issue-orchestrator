# Repo Guardrails (This Repository Only)

These guardrails are **specific to the issue-orchestrator repository**.
They are invoked via `validation.script` in this repo's config.

Other repos can place their validation/guardrails anywhere they want and
simply point `validation.script` at their own implementation.
