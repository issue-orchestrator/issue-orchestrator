# Test Fixture: Hooks Missing

This fixture represents a project WITHOUT Claude Code hooks installed.

Used by tests to verify that the verification correctly detects missing hooks
and reports failure when `--no-verify` is NOT blocked.
