# Configuration for the DCO GitHub App / DCO2.
#
# The repository requires every pull request commit to carry a valid
# Signed-off-by line. Do not add `require.members: false`; maintainers and
# owners must sign off too.
allowRemediationCommits:
  individual: true
allowOverrideAction: false
