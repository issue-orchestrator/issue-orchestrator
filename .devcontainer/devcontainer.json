{
  "name": "issue-orchestrator",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.13-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "22"
    },
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "forwardPorts": [19080, 19081, 8080],
  "portsAttributes": {
    "19080": {
      "label": "Issue Orchestrator Control Center",
      "onAutoForward": "openBrowserOnce",
      "protocol": "http"
    },
    "19081": {
      "label": "Issue Orchestrator Control API",
      "onAutoForward": "silent",
      "protocol": "http"
    },
    "8080": {
      "label": "Issue Orchestrator Engine Dashboard",
      "onAutoForward": "silent",
      "protocol": "http"
    }
  },

  "//lifecycle": [
    "Split deliberately (#7100). A prebuild bakes onCreateCommand and",
    "updateContentCommand but NOT postCreateCommand, so leaving the expensive",
    "setup in postCreateCommand is why enabling prebuilds appears to do nothing.",
    "onCreate: toolchain that does not depend on repo content.",
    "updateContent: lockfile-driven sync — baked by the prebuild AND re-run on",
    "create, which is what closes the drift between a stale prebuild and HEAD.",
    "Both `uv sync --frozen` and `npm ci` converge from any starting state, so",
    "re-running them against a baked tree is cheap and safe.",
    "postStart runs on EVERY resume — keep it near-empty."
  ],
  "onCreateCommand": "npm install -g @openai/codex@0.148.0",
  "updateContentCommand": "make worktree-setup",
  "postCreateCommand": ".devcontainer/seed-agent-onboarding.sh",

  "hostRequirements": {
    "cpus": 4,
    "memory": "8gb",
    "storage": "32gb"
  },
  "customizations": {
    "codespaces": {
      "openFiles": [
        "docs/user/codespaces.md"
      ]
    },
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "charliermarsh.ruff",
        "ms-playwright.playwright",
        "github.vscode-pull-request-github"
      ]
    }
  }
}
